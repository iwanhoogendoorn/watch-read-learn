# `npm version` defaults to a "v" prefix, but release.yml requires the tag to
# equal the manifest version exactly — BRAT looks it up that way. Every tag in
# this repo is bare (1.24.0, not v1.24.0); this keeps the documented release
# command honest instead of producing a tag CI will reject.
tag-version-prefix=""
